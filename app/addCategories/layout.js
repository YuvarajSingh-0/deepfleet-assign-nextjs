export const metadata = {
    title: 'Add Category',
    description: 'Add Categories',
}

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
